import { ageCalc } from ".././src/super-galactic-age-calculator.js"; 

describe("ageCalc(age)", () => { 

  test("should determine if ageCalc object is instantiated correctly", () => {
    expect.ageCalc(30).toBe(30);
  })
})
